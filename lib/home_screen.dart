import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget{
  Widget build(BuildContext context){
    return Scaffold(
      body:Container(
      child: Center(
        child:Text('Welcome to Net Objext Test',style: TextStyle(fontSize: 25,fontWeight: FontWeight.bold),) ,),
    ),

    );
    
  }
}